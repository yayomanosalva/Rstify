import type { Answers } from '../prompts';

export function generateDashboard(templateDir: string, targetDir: string, answers: Answers) {
  // Dashboard template is already in the base template
  // No additional files needed
}
