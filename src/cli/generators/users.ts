import type { Answers } from '../prompts';

export function generateUsers(templateDir: string, targetDir: string, answers: Answers) {
  // Users template is already in the base template
  // No additional files needed
}
